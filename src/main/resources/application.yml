api:
  url:
    company: http://dev.markitondemand.com/MODApis/Api/v2/Lookup/json?input={name}
    quote: http://dev.markitondemand.com/MODApis/Api/v2/Quote/json?symbol={symbol}


pivotal:
  summary:
    quotes: 3
    symbols:
      it: EMC,ORCL,IBM,INTC,AMD,HPQ,CSCO,AAPL
      fs: JPM,MS,BAC,GS,WFC,BK

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

---

spring:
  profiles: oracle
  jpa:
    database-platform: org.hibernate.dialect.Oracle10gDialect
#    database: org.hibernate.dialect.Oracle10gDialect

  datasource:
#    driverClassName=oracle.jdbc.driver.OracleDriver
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@//172.16.168.242:1521/XE
    username: pivotal
    password: pivotal

---

spring:
  profiles: cloud-oracle
  jpa:
    database-platform: org.hibernate.dialect.Oracle10gDialect
#    database: org.hibernate.dialect.Oracle10gDialect

  datasource:
#    driverClassName=oracle.jdbc.driver.OracleDriver
    driver-class-name: oracle.jdbc.driver.OracleDriver

---

spring:
  profiles: cloud
  jpa:
    database: MYSQL
